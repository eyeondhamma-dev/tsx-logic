```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C8S1_NiyamitSamiksha',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C8S1_NiyamitSamiksha: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Clock hand rotation
  const hourRot = interpolate(frame, [0, durationInFrames], [0, 360], { extrapolateRight: 'clamp' });
  const minuteRot = interpolate(frame, [0, durationInFrames], [0, 1440], { extrapolateRight: 'clamp' });

  // Headline reveal
  const headOp = interpolate(frame, [20, 45], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const headY = sp(20) * 0 + interpolate(frame, [20, 45], [60, 0], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // "6 महीने" callout
  const sixScale = sp(50, { damping: 8, stiffness: 200 });
  const sixOp = interpolate(frame, [50, 70], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Particle orbs
  const orbs = Array.from({ length: 14 }, (_, i) => ({
    x: seededRandom(i * 3 + 1) * 1920,
    y: seededRandom(i * 3 + 2) * 1080,
    r: 3 + seededRandom(i * 3 + 3) * 5,
    speed: 0.3 + seededRandom(i * 7) * 0.5,
    phase: seededRandom(i * 5) * Math.PI * 2,
  }));

  // Shape layers
  const drift1X = interpolate(frame, [0, durationInFrames], [-20, 20], { extrapolateRight: 'clamp' });
  const drift1Y = interpolate(frame, [0, durationInFrames], [0, -30], { extrapolateRight: 'clamp' });
  const breathScale = 1 + Math.sin(frame * 0.04) * 0.02;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Gradient Background — deep navy → charcoal */}
      <AbsoluteFill>
        <div style={{
          width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at 30% 40%, #0d1b2a 0%, #1C1C1C 55%, #2D2D2D 100%)',
        }} />
      </AbsoluteFill>

      {/* Shape Layer 1 — large transparent circle drifting */}
      <AbsoluteFill style={{ transform: `translate(${drift1X}px, ${drift1Y}px)` }}>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <circle cx="1500" cy="200" r="380" fill="none" stroke="#e9d700" strokeWidth="1" opacity="0.05" />
          <circle cx="1500" cy="200" r="280" fill="#e9d700" opacity="0.03" transform={`scale(${breathScale})`} style={{ transformOrigin: '1500px 200px' }} />
        </svg>
      </AbsoluteFill>

      {/* Shape Layer 2 — rotating hexagon */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <polygon
            points="200,100 300,50 400,100 400,200 300,250 200,200"
            fill="none" stroke="#FFC107" strokeWidth="1.5" opacity="0.06"
            transform={`rotate(${frame * 0.15}, 300, 150)`}
          />
        </svg>
      </AbsoluteFill>

      {/* Motion Texture — concentric rings clock-style */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
          {[120, 200, 280, 360].map((r, i) => (
            <circle key={i} cx="960" cy="540" r={r} fill="none" stroke="#F5F5F5" strokeWidth="0.8" strokeDasharray="6 14" />
          ))}
          {/* Radial lines */}
          {Array.from({ length: 12 }, (_, i) => {
            const angle = (i / 12) * Math.PI * 2;
            return (
              <line key={i}
                x1={960 + Math.cos(angle) * 100} y1={540 + Math.sin(angle) * 100}
                x2={960 + Math.cos(angle) * 360} y2={540 + Math.sin(angle) * 360}
                stroke="#F5F5F5" strokeWidth="0.5"
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {orbs.map((o, i) => (
            <circle
              key={i}
              cx={o.x + Math.sin(frame * o.speed + o.phase) * 18}
              cy={o.y + Math.cos(frame * o.speed * 0.7 + o.phase) * 12}
              r={o.r}
              fill="#e9d700"
              opacity={0.08 + Math.sin(frame * 0.06 + o.phase) * 0.04}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* ── MAIN CONTENT ── */}

      {/* Left — Animated Clock SVG */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 120, top: '50%',
          transform: 'translateY(-50%)',
          display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 24,
        }}>
          {/* Clock face */}
          <div style={{
            opacity: interpolate(frame, [0, 25], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            transform: `scale(${sp(0, { damping: 14 })})`,
          }}>
            <svg width="280" height="280" viewBox="0 0 280 280">
              {/* Outer ring glow */}
              <circle cx="140" cy="140" r="130" fill="none" stroke="#e9d700" strokeWidth="3" opacity="0.4" />
              <circle cx="140" cy="140" r="125" fill="#2D2D2D" opacity="0.9" />
              {/* Hour markers */}
              {Array.from({ length: 12 }, (_, i) => {
                const a = (i / 12) * Math.PI * 2 - Math.PI / 2;
                return (
                  <line key={i}
                    x1={140 + Math.cos(a) * 100} y1={140 + Math.sin(a) * 100}
                    x2={140 + Math.cos(a) * 115} y2={140 + Math.sin(a) * 115}
                    stroke="#e9d700" strokeWidth={i % 3 === 0 ? 3 : 1.5} opacity="0.7"
                  />
                );
              })}
              {/* Hour hand */}
              <line
                x1="140" y1="140"
                x2={140 + Math.cos((hourRot - 90) * Math.PI / 180) * 65}
                y2={140 + Math.sin((hourRot - 90) * Math.PI / 180) * 65}
                stroke="#F5F5F5" strokeWidth="5" strokeLinecap="round"
              />
              {/* Minute hand */}
              <line
                x1="140" y1="140"
                x2={140 + Math.cos((minuteRot - 90) * Math.PI / 180) * 90}
                y2={140 + Math.sin((minuteRot - 90) * Math.PI / 180) * 90}
                stroke="#e9d700" strokeWidth="3" strokeLinecap="round"
              />
              {/* Center */}
              <circle cx="140" cy="140" r="6" fill="#e9d700" />
            </svg>
          </div>

          {/* "हर 6 महीने" label */}
          <div style={{
            opacity: sixOp,
            transform: `scale(${sixScale})`,
            background: 'linear-gradient(135deg, #e9d700, #FFC107)',
            borderRadius: 16,
            padding: '18px 36px',
            textAlign: 'center',
          }}>
            <div style={{
              fontFamily: 'Yantra One, sans-serif',
              fontSize: 72,
              fontWeight: 900,
              color: '#1C1C1C',
              lineHeight: 1,
            }}>6 महीने</div>
            <div style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 28,
              fontWeight: 700,
              color: '#1C1C1C',
              marginTop: 8,
            }}>समीक्षा करो</div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Right — Headline + Body */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 520, top: '50%',
          transform: 'translateY(-50%)',
          maxWidth: 1100,
        }}>
          {/* Tag */}
          <div style={{
            opacity: interpolate(frame, [10, 30], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            display: 'inline-block',
            background: '#ff0000',
            borderRadius: 8,
            padding: '8px 24px',
            marginBottom: 28,
          }}>
            <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 28, fontWeight: 800, color: '#F5F5F5', letterSpacing: 3 }}>
              FINAL RULE
            </span>
          </div>

          {/* Main Headline */}
          <div style={{
            opacity: headOp,
            transform: `translateY(${headY}px)`,
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 110,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.1,
            marginBottom: 36,
          }}>
            नियमित<br />
            <span style={{ color: '#e9d700' }}>समीक्षा</span> करो
          </div>

          {/* Body */}
          <div style={{
            opacity: interpolate(frame, [45, 70], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            transform: `translateY(${interpolate(frame, [45, 70], [30, 0], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}px)`,
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 52,
            fontWeight: 500,
            color: '#9E9E9E',
            lineHeight: 1.55,
          }}>
            किसी बड़े बदलाव की जरूरत नहीं होगी<br />
            लेकिन <span style={{ color: '#F5F5F5', fontWeight: 700 }}>छोटे समायोजन</span> जरूरी हैं
          </div>
        </div>
      </AbsoluteFill>

      {/* Stick Figure — Buffett character */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          right: 60, bottom: 180,
          opacity: interpolate(frame, [40, 60], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          transform: `
            translateY(${Math.sin(frame * 0.05) * 3}px)
            scale(${sp(40, { damping: 10 })})
          `,
        }}>
          <svg width="110" height="180" viewBox="0 0 110 180">
            {/* Golden glow */}
            <ellipse cx="55" cy="90" rx="50" ry="80" fill="#e9d700" opacity="0.07" />
            {/* Head */}
            <circle cx="55" cy="28" r="22"
              fill="none" stroke="#F5F5F5" strokeWidth="3"
              style={{ transform: `rotate(${Math.sin(frame * 0.04) * 3}deg)`, transformOrigin: '55px 28px' }}
            />
            {/* Golden glasses */}
            <rect x="36" y="24" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <rect x="58" y="24" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <line x1="50" y1="28" x2="58" y2="28" stroke="#e9d700" strokeWidth="2" />
            {/* Body */}
            <line x1="55" y1="50" x2="55" y2="115" stroke="#F5F5F5" strokeWidth="3" />
            {/* Arms — holding clipboard */}
            <line x1="55" y1="70" x2="25" y2="95" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="55" y1="70" x2="85" y2="85" stroke="#F5F5F5" strokeWidth="3" />
            {/* Clipboard */}
            <rect x="10" y="90" width="28" height="36" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
            <line x1="15" y1="100" x2="34" y2="100" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="15" y1="108" x2="34" y2="108" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="15" y1="116" x2="28" y2="116" stroke="#e9d700" strokeWidth="1.5" />
            {/* Legs */}
            <line x1="55" y1="115" x2="35" y2="155" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="55" y1="115" x2="75" y2="155" stroke="#F5F5F5" strokeWidth="3" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.65) 100%)',
        }} />
      </AbsoluteFill>

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: interpolate(frame, [0, 20], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C8S1_NiyamitSamiksha;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C8S2_SahiSalahakar',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C8S2_SahiSalahakar: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Bar chart — stocks grown too large
  const bars = [
    { label: 'Equity', pct: 0.72, color: '#ff0000' },
    { label: 'Bonds', pct: 0.15, color: '#e9d700' },
    { label: 'Cash', pct: 0.13, color: '#9E9E9E' },
  ];
  const barReveal = interpolate(frame, [20, 60], [0, 1], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Warning pulse on equity bar
  const warnPulse = 0.6 + Math.sin(frame * 0.18) * 0.4;

  // Right panel — advisor card
  const cardScale = sp(55, { damping: 10, stiffness: 150 });
  const cardOp = interpolate(frame, [55, 80], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Rebalance arrow
  const arrowProg = interpolate(frame, [80, 110], [0, 1], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Geometric shapes
  const triRot = frame * 0.2;
  const particles = Array.from({ length: 12 }, (_, i) => ({
    x: seededRandom(i * 4 + 1) * 1920,
    y: seededRandom(i * 4 + 2) * 1080,
    r: 2 + seededRandom(i * 4 + 3) * 4,
    phase: seededRandom(i * 9) * Math.PI * 2,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Background — warm dark amber gradient */}
      <AbsoluteFill>
        <div style={{
          width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at 70% 30%, #1a1200 0%, #1C1C1C 50%, #0e0e0e 100%)',
        }} />
      </AbsoluteFill>

      {/* Shape Layer — large transparent triangle */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <polygon
            points="1800,50 1920,400 1680,400"
            fill="#e9d700" opacity="0.04"
            transform={`rotate(${triRot}, 1800, 250)`}
          />
          <rect x="0" y="800" width="500" height="280" rx="0"
            fill="#FFC107" opacity="0.03"
            transform={`rotate(-${triRot * 0.5}, 250, 940)`}
          />
        </svg>
      </AbsoluteFill>

      {/* Motion Texture — grid lines */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.03 }}>
          {Array.from({ length: 20 }, (_, i) => (
            <line key={`h${i}`} x1="0" y1={i * 56} x2="1920" y2={i * 56} stroke="#F5F5F5" strokeWidth="0.5" />
          ))}
          {Array.from({ length: 35 }, (_, i) => (
            <line key={`v${i}`} x1={i * 56} y1="0" x2={i * 56} y2="1080" stroke="#F5F5F5" strokeWidth="0.5" />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating Particles */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {particles.map((p, i) => (
            <circle key={i}
              cx={p.x + Math.sin(frame * 0.04 + p.phase) * 15}
              cy={p.y + Math.cos(frame * 0.03 + p.phase) * 10}
              r={p.r} fill="#FFC107"
              opacity={0.06 + Math.sin(frame * 0.07 + p.phase) * 0.03}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* ── MAIN CONTENT ── */}

      {/* Left — Bar Chart "Portfolio Imbalance" */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 100, top: '50%',
          transform: 'translateY(-50%)',
          width: 600,
        }}>
          {/* Title */}
          <div style={{
            opacity: interpolate(frame, [0, 20], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            fontFamily: 'Inter, sans-serif',
            fontSize: 30,
            fontWeight: 700,
            color: '#9E9E9E',
            letterSpacing: 4,
            marginBottom: 40,
            textTransform: 'uppercase',
          }}>
            Portfolio Imbalance
          </div>

          {/* Bars */}
          {bars.map((b, i) => (
            <div key={i} style={{
              marginBottom: 32,
              opacity: interpolate(frame, [15 + i * 10, 35 + i * 10], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            }}>
              <div style={{
                display: 'flex', justifyContent: 'space-between', marginBottom: 10,
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 34,
                fontWeight: 700,
                color: b.color,
              }}>
                <span>{b.label}</span>
                <span style={{ opacity: i === 0 ? warnPulse : 1 }}>
                  {Math.round(b.pct * 100 * barReveal)}%
                </span>
              </div>
              <div style={{ background: '#2D2D2D', borderRadius: 8, height: 28, overflow: 'hidden' }}>
                <div style={{
                  width: `${b.pct * 100 * barReveal}%`,
                  height: '100%',
                  background: i === 0
                    ? `linear-gradient(90deg, #ff0000, #ff4444)`
                    : b.color,
                  borderRadius: 8,
                  boxShadow: i === 0 ? `0 0 ${12 * warnPulse}px ${b.color}66` : 'none',
                  transition: 'width 0.1s',
                }} />
              </div>
              {/* Warning badge on equity */}
              {i === 0 && (
                <div style={{
                  marginTop: 8,
                  opacity: interpolate(frame, [60, 75], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
                  display: 'flex', alignItems: 'center', gap: 10,
                }}>
                  <svg width="22" height="22" viewBox="0 0 22 22">
                    <polygon points="11,2 21,20 1,20" fill="#ff0000" opacity="0.9" />
                    <text x="11" y="17" textAnchor="middle" fill="#F5F5F5" fontSize="12" fontWeight="900">!</text>
                  </svg>
                  <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 26, color: '#ff0000', fontWeight: 600 }}>
                    बहुत ज्यादा — Rebalance करो
                  </span>
                </div>
              )}
            </div>
          ))}
        </div>
      </AbsoluteFill>

      {/* Rebalance Arrow */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 720, top: '50%',
          transform: 'translateY(-50%)',
          opacity: arrowProg,
        }}>
          <svg width="120" height="80" viewBox="0 0 120 80">
            <defs>
              <marker id="ah" markerWidth="10" markerHeight="7" refX="10" refY="3.5" orient="auto">
                <polygon points="0 0, 10 3.5, 0 7" fill="#e9d700" />
              </marker>
            </defs>
            <line
              x1="0" y1="40"
              x2={120 * arrowProg} y2="40"
              stroke="#e9d700" strokeWidth="4"
              markerEnd="url(#ah)"
            />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Right — Advisor Card */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', right: 100, top: '50%',
          transform: `translateY(-50%) scale(${cardScale})`,
          opacity: cardOp,
          width: 580,
        }}>
          <div style={{
            background: 'linear-gradient(135deg, #2D2D2D, #1a1a1a)',
            border: '2px solid #e9d700',
            borderRadius: 24,
            padding: '50px 48px',
            boxShadow: '0 0 60px rgba(233,215,0,0.12)',
          }}>
            {/* Icon */}
            <div style={{ marginBottom: 28 }}>
              <svg width="64" height="64" viewBox="0 0 64 64">
                <circle cx="32" cy="22" r="14" fill="none" stroke="#e9d700" strokeWidth="3" />
                <path d="M8 54 C8 38 56 38 56 54" fill="none" stroke="#e9d700" strokeWidth="3" />
                <circle cx="32" cy="22" r="6" fill="#e9d700" opacity="0.4" />
                {/* Checkmark badge */}
                <circle cx="52" cy="12" r="10" fill="#1C1C1C" stroke="#00cc66" strokeWidth="2" />
                <polyline points="47,12 51,16 57,8" fill="none" stroke="#00cc66" strokeWidth="2.5" strokeLinecap="round" />
              </svg>
            </div>

            <div style={{
              fontFamily: 'Yantra One, sans-serif',
              fontSize: 62,
              fontWeight: 900,
              color: '#F5F5F5',
              lineHeight: 1.15,
              marginBottom: 24,
            }}>
              सही<br />
              <span style={{ color: '#e9d700' }}>सलाहकार</span><br />
              ढूंढो
            </div>

            {/* Criteria list */}
            {[
              'तुम्हारे हित में काम करे',
              'Commission नहीं, Fee-based हो',
              'सरल भाषा में समझाए',
            ].map((txt, i) => (
              <div key={i} style={{
                display: 'flex', alignItems: 'center', gap: 16,
                marginBottom: 18,
                opacity: interpolate(frame, [75 + i * 12, 95 + i * 12], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
                transform: `translateX(${interpolate(frame, [75 + i * 12, 95 + i * 12], [30, 0], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}px)`,
              }}>
                <svg width="24" height="24" viewBox="0 0 24 24">
                  <circle cx="12" cy="12" r="11" fill="#e9d700" opacity="0.15" />
                  <polyline points="5,12 10,17 19,7" fill="none" stroke="#e9d700" strokeWidth="2.5" strokeLinecap="round" />
                </svg>
                <span style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 36, color: '#F5F5F5', fontWeight: 500 }}>
                  {txt}
                </span>
              </div>
            ))}
          </div>
        </div>
      </AbsoluteFill>

      {/* Stick Figure */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', right: 70, bottom: 160,
          opacity: interpolate(frame, [50, 65], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          transform: `translateY(${Math.sin(frame * 0.05) * 3}px) scale(${sp(50, { damping: 10 })})`,
        }}>
          <svg width="90" height="150" viewBox="0 0 90 150">
            <ellipse cx="45" cy="75" rx="40" ry="65" fill="#e9d700" opacity="0.07" />
            <circle cx="45" cy="22" r="18" fill="none" stroke="#F5F5F5" strokeWidth="2.5"
              style={{ transform: `rotate(${Math.sin(frame * 0.04) * 3}deg)`, transformOrigin: '45px 22px' }}
            />
            <rect x="30" y="18" width="11" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
            <rect x="48" y="18" width="11" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
            <line x1="41" y1="21" x2="48" y2="21" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="45" y1="40" x2="45" y2="95" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="60" x2="20" y2="78" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="60" x2="70" y2="72" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="95" x2="28" y2="130" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="95" x2="62" y2="130" stroke="#F5F5F5" strokeWidth="2.5" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.7) 100%)',
        }} />
      </AbsoluteFill>

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C8S2_SahiSalahakar;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C8S3_BhagJao',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C8S3_BhagJao: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Screen shake on "भाग जाओ" reveal
  const shakeFrame = frame - 45;
  const shakeX = shakeFrame >= 0 && shakeFrame < 8
    ? Math.sin(shakeFrame * 3.5) * (8 - shakeFrame) * 2.5
    : 0;
  const shakeY = shakeFrame >= 0 && shakeFrame < 8
    ? Math.cos(shakeFrame * 4) * (8 - shakeFrame) * 1.5
    : 0;

  // Red X reveal
  const xScale = sp(45, { damping: 7, stiffness: 300 });
  const xOp = interpolate(frame, [45, 55], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // "भाग जाओ" text
  const bhagScale = sp(52, { damping: 6, stiffness: 250 });

  // Green panel — good advisor
  const greenOp = interpolate(frame, [85, 110], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const greenX = interpolate(frame, [85, 110], [80, 0], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Floating danger particles (red)
  const dangerParts = Array.from({ length: 10 }, (_, i) => ({
    x: 80 + seededRandom(i * 5 + 1) * 700,
    y: seededRandom(i * 5 + 2) * 1080,
    phase: seededRandom(i * 5 + 3) * Math.PI * 2,
    r: 3 + seededRandom(i * 5 + 4) * 5,
  }));

  return (
    <AbsoluteFill style={{
      backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp,
      transform: `translate(${shakeX}px, ${shakeY}px)`,
    }}>

      {/* Background — split */}
      <AbsoluteFill>
        {/* Left — dark red tint */}
        <div style={{
          position: 'absolute', left: 0, top: 0,
          width: '50%', height: '100%',
          background: 'linear-gradient(135deg, #1a0000 0%, #1C1C1C 100%)',
        }} />
        {/* Right — dark green tint */}
        <div style={{
          position: 'absolute', right: 0, top: 0,
          width: '50%', height: '100%',
          background: 'linear-gradient(135deg, #1C1C1C 0%, #001a08 100%)',
          opacity: greenOp,
        }} />
        {/* Divider line */}
        <div style={{
          position: 'absolute', left: '50%', top: 0,
          width: 3, height: '100%',
          background: 'linear-gradient(180deg, transparent, #9E9E9E44, transparent)',
          opacity: greenOp,
        }} />
      </AbsoluteFill>

      {/* Shape Layer — large diagonal stripes */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
          {Array.from({ length: 8 }, (_, i) => (
            <line key={i}
              x1={i * 140 - 200} y1="0"
              x2={i * 140 + 300} y2="1080"
              stroke="#ff0000" strokeWidth="40"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Motion Texture — diagonal lines right side */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.03 }}>
          {Array.from({ length: 12 }, (_, i) => (
            <line key={i}
              x1={960 + i * 90} y1="0"
              x2={960 + i * 90 + 200} y2="1080"
              stroke="#00cc66" strokeWidth="1"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Red danger particles */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {dangerParts.map((p, i) => (
            <circle key={i}
              cx={p.x + Math.sin(frame * 0.06 + p.phase) * 14}
              cy={p.y + Math.cos(frame * 0.04 + p.phase) * 9}
              r={p.r} fill="#ff0000"
              opacity={0.07 + Math.sin(frame * 0.1 + p.phase) * 0.04}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* ── LEFT SIDE — BAD ADVISOR ── */}

      {/* "जटिल है" label */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 100, top: 160,
          opacity: interpolate(frame, [0, 20], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          <div style={{
            background: '#ff000022',
            border: '2px solid #ff000055',
            borderRadius: 12,
            padding: '16px 32px',
            display: 'inline-block',
          }}>
            <span style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 40,
              color: '#ff0000',
              fontWeight: 700,
            }}>
              "ये जटिल है — बस मुझ पर विश्वास करो"
            </span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Big Red X + BHAG JAO */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 80, top: '50%',
          transform: `translateY(-50%)`,
          display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 20,
        }}>
          {/* Giant X */}
          <div style={{
            opacity: xOp,
            transform: `scale(${xScale})`,
          }}>
            <svg width="220" height="220" viewBox="0 0 220 220">
              <circle cx="110" cy="110" r="105" fill="#ff000015" stroke="#ff0000" strokeWidth="4" />
              <line x1="55" y1="55" x2="165" y2="165" stroke="#ff0000" strokeWidth="14" strokeLinecap="round" />
              <line x1="165" y1="55" x2="55" y2="165" stroke="#ff0000" strokeWidth="14" strokeLinecap="round" />
            </svg>
          </div>

          {/* BHAG JAO text */}
          <div style={{
            transform: `scale(${bhagScale})`,
            opacity: interpolate(frame, [52, 65], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 140,
            fontWeight: 900,
            color: '#ff0000',
            letterSpacing: -2,
            textShadow: '0 0 40px rgba(255,0,0,0.5)',
          }}>
            भाग जाओ!
          </div>

          <div style={{
            opacity: interpolate(frame, [65, 80], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 44,
            color: '#9E9E9E',
            textAlign: 'center',
            maxWidth: 700,
          }}>
            जो समझाए नहीं — वो भरोसेमंद नहीं
          </div>
        </div>
      </AbsoluteFill>

      {/* ── RIGHT SIDE — GOOD ADVISOR ── */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', right: 80, top: '50%',
          transform: `translateY(-50%) translateX(${greenX}px)`,
          opacity: greenOp,
          width: 700,
        }}>
          <div style={{
            background: 'linear-gradient(135deg, #001a08, #0d2a15)',
            border: '2px solid #00cc6688',
            borderRadius: 28,
            padding: '50px 52px',
          }}>
            <div style={{ marginBottom: 32 }}>
              <svg width="60" height="60" viewBox="0 0 60 60">
                <circle cx="30" cy="30" r="28" fill="none" stroke="#00cc66" strokeWidth="3" />
                <polyline points="15,30 26,42 48,18" fill="none" stroke="#00cc66" strokeWidth="4" strokeLinecap="round" />
              </svg>
            </div>
            <div style={{
              fontFamily: 'Yantra One, sans-serif',
              fontSize: 72,
              fontWeight: 900,
              color: '#F5F5F5',
              lineHeight: 1.15,
              marginBottom: 30,
            }}>
              सच्चा<br />
              <span style={{ color: '#00cc66' }}>सलाहकार</span>
            </div>
            {[
              'सरल भाषा में बताएगा',
              'हर सवाल का जवाब देगा',
              'तुम्हारे लिए काम करेगा',
            ].map((t, i) => (
              <div key={i} style={{
                display: 'flex', gap: 16, alignItems: 'center',
                marginBottom: 18,
                opacity: interpolate(frame, [100 + i * 10, 118 + i * 10], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
              }}>
                <div style={{
                  width: 10, height: 10, borderRadius: '50%',
                  background: '#00cc66', flexShrink: 0,
                }} />
                <span style={{
                  fontFamily: 'Noto Sans Devanagari, sans-serif',
                  fontSize: 38, color: '#F5F5F5', fontWeight: 500,
                }}>
                  {t}
                </span>
              </div>
            ))}
          </div>
        </div>
      </AbsoluteFill>

      {/* Stick Figure — running away (left side) */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          left: interpolate(frame, [45, 85], [300, 520], { easing: Easing.inOut(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          bottom: 150,
          opacity: interpolate(frame, [40, 55], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          transform: `translateY(${Math.sin(frame * 0.12) * 6}px)`,
        }}>
          <svg width="90" height="150" viewBox="0 0 90 150">
            <ellipse cx="45" cy="75" rx="38" ry="62" fill="#ff0000" opacity="0.06" />
            <circle cx="45" cy="22" r="18" fill="none" stroke="#F5F5F5" strokeWidth="2.5"
              style={{ transform: `rotate(${Math.sin(frame * 0.15) * 8}deg)`, transformOrigin: '45px 22px' }}
            />
            <rect x="30" y="18" width="11" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
            <rect x="48" y="18" width="11" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
            <line x1="41" y1="21" x2="48" y2="21" stroke="#e9d700" strokeWidth="1.5" />
            <line x1="45" y1="40" x2="45" y2="95" stroke="#F5F5F5" strokeWidth="2.5" />
            {/* Running arms */}
            <line x1="45" y1="60"
              x2={45 + Math.sin(frame * 0.2) * 28}
              y2={60 + Math.cos(frame * 0.2) * 18}
              stroke="#F5F5F5" strokeWidth="2.5"
            />
            <line x1="45" y1="60"
              x2={45 - Math.sin(frame * 0.2) * 28}
              y2={60 - Math.cos(frame * 0.2) * 18}
              stroke="#F5F5F5" strokeWidth="2.5"
            />
            {/* Running legs */}
            <line x1="45" y1="95"
              x2={45 - Math.sin(frame * 0.2) * 22}
              y2={95 + 35 + Math.cos(frame * 0.2) * 10}
              stroke="#F5F5F5" strokeWidth="2.5"
            />
            <line x1="45" y1="95"
              x2={45 + Math.sin(frame * 0.2) * 22}
              y2={95 + 35 - Math.cos(frame * 0.2) * 10}
              stroke="#F5F5F5" strokeWidth="2.5"
            />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.7) 100%)',
        }} />
      </AbsoluteFill>

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C8S3_BhagJao;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C8S4_AapParVishwas',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C8S4_AapParVishwas: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Mirror / reflection reveal
  const mirrorReveal = interpolate(frame, [15, 55], [0, 1], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Gold glow pulse
  const glowPulse = 0.5 + Math.sin(frame * 0.07) * 0.5;

  // Big word reveal — staggered letters simulation
  const word1Op = interpolate(frame, [20, 40], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const word1Y = sp(20) * 0 + interpolate(frame, [20, 40], [80, 0], { easing: Easing.out(Easing.exp), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const word2Op = interpolate(frame, [35, 55], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const word2Y = interpolate(frame, [35, 55], [80, 0], { easing: Easing.out(Easing.exp), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const word3Op = interpolate(frame, [50, 70], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Expanding rings
  const ringRadius = interpolate(frame, [0, durationInFrames], [0, 900], { extrapolateRight: 'clamp' });

  // Particles — golden
  const particles = Array.from({ length: 20 }, (_, i) => ({
    angle: (i / 20) * Math.PI * 2 + frame * 0.02,
    radius: 180 + seededRandom(i * 3) * 120,
    size: 2 + seededRandom(i * 7) * 5,
    phase: seededRandom(i * 11) * Math.PI * 2,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Background — deep gold radial */}
      <AbsoluteFill>
        <div style={{
          width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at 50% 50%, #1a1400 0%, #1C1C1C 60%, #0a0a0a 100%)',
        }} />
      </AbsoluteFill>

      {/* Expanding ring texture */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {[0, 1, 2, 3].map((i) => (
            <circle key={i}
              cx="960" cy="540"
              r={Math.max(0, ringRadius - i * 180)}
              fill="none"
              stroke="#e9d700"
              strokeWidth="1"
              opacity={Math.max(0, (0.06 - i * 0.015) * mirrorReveal)}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Shape Layer — large diamond */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <polygon
            points="960,140 1280,540 960,940 640,540"
            fill="none" stroke="#e9d700" strokeWidth="1.5"
            opacity={0.05 * mirrorReveal}
          />
          <polygon
            points="960,240 1180,540 960,840 740,540"
            fill="#e9d700"
            opacity={0.03 * mirrorReveal}
          />
        </svg>
      </AbsoluteFill>

      {/* Orbiting golden particles */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {particles.map((p, i) => (
            <circle key={i}
              cx={960 + Math.cos(p.angle) * p.radius}
              cy={540 + Math.sin(p.angle) * p.radius * 0.6}
              r={p.size}
              fill="#e9d700"
              opacity={(0.15 + Math.sin(frame * 0.08 + p.phase) * 0.1) * mirrorReveal}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* CENTER — Main Message */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', top: '50%', left: '50%',
          transform: 'translate(-50%, -50%)',
          textAlign: 'center',
        }}>
          {/* Gold glow behind */}
          <div style={{
            position: 'absolute', top: '50%', left: '50%',
            transform: 'translate(-50%, -50%)',
            width: 500, height: 500,
            borderRadius: '50%',
            background: `radial-gradient(circle, rgba(233,215,0,${0.08 * glowPulse}) 0%, transparent 70%)`,
            pointerEvents: 'none',
          }} />

          {/* Subtext */}
          <div style={{
            opacity: interpolate(frame, [5, 18], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            fontFamily: 'Inter, sans-serif',
            fontSize: 30,
            fontWeight: 700,
            color: '#9E9E9E',
            letterSpacing: 6,
            marginBottom: 24,
            textTransform: 'uppercase',
          }}>
            ये सब करने के बाद
          </div>

          {/* VISHWAS */}
          <div style={{ overflow: 'hidden', marginBottom: 8 }}>
            <div style={{
              opacity: word1Op,
              transform: `translateY(${word1Y}px)`,
              fontFamily: 'Yantra One, sans-serif',
              fontSize: 230,
              fontWeight: 900,
              color: '#F5F5F5',
              lineHeight: 1,
              letterSpacing: -4,
            }}>
              विश्वास
            </div>
          </div>

          <div style={{ overflow: 'hidden', marginBottom: 20 }}>
            <div style={{
              opacity: word2Op,
              transform: `translateY(${word2Y}px)`,
              fontFamily: 'Yantra One, sans-serif',
              fontSize: 110,
              fontWeight: 900,
              color: '#e9d700',
              lineHeight: 1,
            }}>
              खुद पर
            </div>
          </div>

          <div style={{
            opacity: word3Op,
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 50,
            color: '#9E9E9E',
            fontWeight: 400,
            lineHeight: 1.5,
          }}>
            एक चीज़ जो हमेशा बची रहती है
          </div>
        </div>
      </AbsoluteFill>

      {/* Mirror SVG — left & right */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 60, top: '50%',
          transform: 'translateY(-50%)',
          opacity: interpolate(frame, [30, 55], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          <svg width="80" height="160" viewBox="0 0 80 160">
            <ellipse cx="40" cy="80" rx="35" ry="70" fill="none" stroke="#e9d700" strokeWidth="2" opacity="0.4" />
            <line x1="40" y1="10" x2="40" y2="150" stroke="#e9d700" strokeWidth="1" opacity="0.2" />
          </svg>
        </div>
        <div style={{
          position: 'absolute', right: 60, top: '50%',
          transform: 'translateY(-50%)',
          opacity: interpolate(frame, [30, 55], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          <svg width="80" height="160" viewBox="0 0 80 160">
            <ellipse cx="40" cy="80" rx="35" ry="70" fill="none" stroke="#e9d700" strokeWidth="2" opacity="0.4" />
            <line x1="40" y1="10" x2="40" y2="150" stroke="#e9d700" strokeWidth="1" opacity="0.2" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Stick Figure — center, arms open */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', right: 80, bottom: 120,
          opacity: interpolate(frame, [45, 60], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          transform: `translateY(${Math.sin(frame * 0.05) * 3}px) scale(${sp(45, { damping: 10 })})`,
        }}>
          <svg width="100" height="165" viewBox="0 0 100 165">
            <ellipse cx="50" cy="82" rx="46" ry="72" fill="#e9d700" opacity={0.05 + glowPulse * 0.04} />
            <circle cx="50" cy="24" r="20" fill="none" stroke="#F5F5F5" strokeWidth="2.8"
              style={{ transform: `rotate(${Math.sin(frame * 0.04) * 3}deg)`, transformOrigin: '50px 24px' }}
            />
            <rect x="33" y="20" width="12" height="8" rx="4" fill="none" stroke="#e9d700" strokeWidth="2" />
            <rect x="53" y="20" width="12" height="8" rx="4" fill="none" stroke="#e9d700" strokeWidth="2" />
            <line x1="45" y1="24" x2="53" y2="24" stroke="#e9d700" strokeWidth="1.8" />
            {/* Smile */}
            <path d="M 43 33 Q 50 38 57 33" fill="none" stroke="#F5F5F5" strokeWidth="1.5" strokeLinecap="round" />
            <line x1="50" y1="44" x2="50" y2="105" stroke="#F5F5F5" strokeWidth="2.8" />
            {/* Arms open wide */}
            <line x1="50" y1="65"
              x2={50 - 40 - Math.sin(frame * 0.06) * 5}
              y2={80 + Math.cos(frame * 0.06) * 5}
              stroke="#F5F5F5" strokeWidth="2.8"
            />
            <line x1="50" y1="65"
              x2={50 + 40 + Math.sin(frame * 0.06) * 5}
              y2={80 + Math.cos(frame * 0.06) * 5}
              stroke="#F5F5F5" strokeWidth="2.8"
            />
            <line x1="50" y1="105" x2="30" y2="145" stroke="#F5F5F5" strokeWidth="2.8" />
            <line x1="50" y1="105" x2="70" y2="145" stroke="#F5F5F5" strokeWidth="2.8" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.75) 100%)',
        }} />
      </AbsoluteFill>

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C8S4_AapParVishwas;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C8S5_JeevanBharAnubhav',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C8S5_JeevanBharAnubhav: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Timeline / life journey horizontal scroll
  const timelineX = interpolate(frame, [20, 100], [400, 0], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Life milestones
  const milestones = [
    { age: '20s', label: 'पहली नौकरी', icon: '💼', color: '#9E9E9E' },
    { age: '30s', label: 'परिवार', icon: '🏠', color: '#9E9E9E' },
    { age: '40s', label: 'बचत', icon: '💰', color: '#FFC107' },
    { age: '50s', label: 'निवेश', icon: '📈', color: '#FFC107' },
    { age: '70+', label: 'अनुभव', icon: '⭐', color: '#e9d700' },
  ];

  // Star burst
  const starPulse = 0.8 + Math.sin(frame * 0.1) * 0.2;

  // Floating wisdom particles
  const wisdomParts = Array.from({ length: 16 }, (_, i) => ({
    x: seededRandom(i * 6 + 1) * 1920,
    y: seededRandom(i * 6 + 2) * 1080,
    r: 2 + seededRandom(i * 6 + 3) * 4,
    phase: seededRandom(i * 6 + 4) * Math.PI * 2,
    vy: -0.3 - seededRandom(i * 6 + 5) * 0.4,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Background — warm aged parchment feel */}
      <AbsoluteFill>
        <div style={{
          width: '100%', height: '100%',
          background: 'linear-gradient(160deg, #12100a 0%, #1C1C1C 40%, #0f1520 100%)',
        }} />
      </AbsoluteFill>

      {/* Shape Layer — large arc curves */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <path d="M -100,1080 Q 960,-200 2020,1080" fill="none" stroke="#e9d700" strokeWidth="1" opacity="0.05" />
          <path d="M -100,900 Q 960,100 2020,900" fill="none" stroke="#FFC107" strokeWidth="1" opacity="0.04" />
          <circle cx="1700" cy="150" r="200" fill="none" stroke="#e9d700" strokeWidth="1" opacity="0.04"
            transform={`rotate(${frame * 0.08}, 1700, 150)`}
          />
        </svg>
      </AbsoluteFill>

      {/* Motion Texture — dots grid */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
          {Array.from({ length: 20 }, (_, row) =>
            Array.from({ length: 36 }, (_, col) => (
              <circle key={`${row}-${col}`} cx={col * 54 + 27} cy={row * 54 + 27} r="1.5" fill="#F5F5F5" />
            ))
          )}
        </svg>
      </AbsoluteFill>

      {/* Floating particles — rising */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {wisdomParts.map((p, i) => (
            <circle key={i}
              cx={p.x + Math.sin(frame * 0.03 + p.phase) * 20}
              cy={(p.y + frame * p.vy * 0.8) % 1080}
              r={p.r} fill="#e9d700"
              opacity={0.08 + Math.sin(frame * 0.05 + p.phase) * 0.04}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* TOP — Hero Text */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', top: 100, left: 100,
          opacity: interpolate(frame, [0, 20], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          transform: `translateY(${interpolate(frame, [0, 20], [-40, 0], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}px)`,
        }}>
          <div style={{
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 120,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1,
          }}>
            जीवन भर का<br />
            <span style={{ color: '#e9d700' }}>अनुभव</span> — तुम्हारे पास है
          </div>
        </div>
      </AbsoluteFill>

      {/* TIMELINE — horizontal scroll */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', bottom: 200, left: 0, right: 0,
          transform: `translateX(${timelineX}px)`,
        }}>
          {/* Line */}
          <div style={{
            position: 'absolute', top: 55, left: 80, right: 80,
            height: 4,
            background: 'linear-gradient(90deg, #2D2D2D, #e9d700, #2D2D2D)',
            borderRadius: 2,
          }} />

          {/* Milestones */}
          <div style={{
            display: 'flex', alignItems: 'flex-start',
            justifyContent: 'space-around',
            padding: '0 80px',
            gap: 0,
          }}>
            {milestones.map((m, i) => (
              <div key={i} style={{
                display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 16,
                opacity: interpolate(frame, [25 + i * 12, 45 + i * 12], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
                transform: `scale(${sp(25 + i * 12, { damping: 9 })}) ${i === 4 ? `scale(${starPulse})` : ''}`,
              }}>
                {/* Dot on line */}
                <div style={{
                  width: i === 4 ? 28 : 18,
                  height: i === 4 ? 28 : 18,
                  borderRadius: '50%',
                  background: m.color,
                  border: `3px solid ${m.color}`,
                  boxShadow: i === 4 ? `0 0 20px ${m.color}` : 'none',
                  marginBottom: 4,
                }} />

                {/* Age badge */}
                <div style={{
                  background: i === 4 ? '#e9d700' : '#2D2D2D',
                  color: i === 4 ? '#1C1C1C' : '#F5F5F5',
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: i === 4 ? 46 : 38,
                  fontWeight: 800,
                  padding: '10px 24px',
                  borderRadius: 12,
                  border: `2px solid ${m.color}`,
                }}>
                  {m.age}
                </div>

                {/* Label */}
                <div style={{
                  fontFamily: 'Noto Sans Devanagari, sans-serif',
                  fontSize: 34,
                  color: m.color,
                  fontWeight: 700,
                  textAlign: 'center',
                }}>
                  {m.label}
                </div>
              </div>
            ))}
          </div>
        </div>
      </AbsoluteFill>

      {/* Two wisdom cards */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', bottom: 50, left: 100, right: 160,
          display: 'flex', gap: 32,
          opacity: interpolate(frame, [90, 115], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          transform: `translateY(${interpolate(frame, [90, 115], [40, 0], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}px)`,
        }}>
          {[
            { text: 'क्या महत्वपूर्ण है — तुम्हें पता है', icon: '✓' },
            { text: 'तुम क्या वहन कर सकते हो — तुम्हें पता है', icon: '✓' },
          ].map((c, i) => (
            <div key={i} style={{
              flex: 1,
              background: '#2D2D2D',
              border: '1px solid #e9d70033',
              borderRadius: 16,
              padding: '20px 28px',
              display: 'flex', alignItems: 'center', gap: 18,
            }}>
              <div style={{
                width: 36, height: 36,
                background: '#e9d700',
                borderRadius: '50%',
                display: 'flex', alignItems: 'center', justifyContent: 'center',
                fontFamily: 'Inter, sans-serif',
                fontSize: 20, fontWeight: 900, color: '#1C1C1C',
                flexShrink: 0,
              }}>
                {c.icon}
              </div>
              <span style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: 38, color: '#F5F5F5', fontWeight: 500,
              }}>
                {c.text}
              </span>
            </div>
          ))}
        </div>
      </AbsoluteFill>

      {/* Stick Figure — wise elder pose */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', right: 80, top: 80,
          opacity: interpolate(frame, [40, 60], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          transform: `translateY(${Math.sin(frame * 0.05) * 3}px) scale(${sp(40)})`,
        }}>
          <svg width="110" height="185" viewBox="0 0 110 185">
            <ellipse cx="55" cy="92" rx="50" ry="80" fill="#e9d700" opacity={0.06 + starPulse * 0.03} />
            <circle cx="55" cy="24" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3"
              style={{ transform: `rotate(${Math.sin(frame * 0.04) * 3}deg)`, transformOrigin: '55px 24px' }}
            />
            <rect x="36" y="20" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <rect x="58" y="20" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <line x1="50" y1="24" x2="58" y2="24" stroke="#e9d700" strokeWidth="2" />
            {/* Wise smile */}
            <path d="M 45 33 Q 55 40 65 33" fill="none" stroke="#F5F5F5" strokeWidth="1.8" strokeLinecap="round" />
            <line x1="55" y1="46" x2="55" y2="115" stroke="#F5F5F5" strokeWidth="3" />
            {/* One arm pointing forward */}
            <line x1="55" y1="70" x2="90" y2="60" stroke="#F5F5F5" strokeWidth="3" />
            {/* Other arm with cane */}
            <line x1="55" y1="70" x2="25" y2="100" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="25" y1="100" x2="20" y2="140" stroke="#F5F5F5" strokeWidth="2" />
            <line x1="15" y1="140" x2="28" y2="140" stroke="#e9d700" strokeWidth="3" />
            <line x1="55" y1="115" x2="38" y2="158" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="55" y1="115" x2="72" y2="158" stroke="#F5F5F5" strokeWidth="3" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.65) 100%)',
        }} />
      </AbsoluteFill>

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C8S5_JeevanBharAnubhav;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C8S6_AantarikAawaaz',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C8S6_AantarikAawaaz: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Sound wave animation
  const waveAmp = 40 + Math.sin(frame * 0.08) * 15;

  // Gut / inner voice visualization — concentric ripples
  const ripple1 = (frame * 3) % 180;
  const ripple2 = (frame * 3 + 60) % 180;
  const ripple3 = (frame * 3 + 120) % 180;

  // "असुविधाजनक" X card
  const xCardOp = interpolate(frame, [20, 40], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const xCardX = interpolate(frame, [20, 40], [-80, 0], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // "न करो" reveal
  const noReveal = sp(60, { damping: 7, stiffness: 300 });

  // Right panel — gut check visual
  const rightOp = interpolate(frame, [75, 100], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Particles
  const parts = Array.from({ length: 14 }, (_, i) => ({
    x: seededRandom(i * 5 + 1) * 1920,
    y: seededRandom(i * 5 + 2) * 1080,
    r: 2 + seededRandom(i * 5 + 3) * 4,
    phase: seededRandom(i * 5 + 4) * Math.PI * 2,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Background — deep purple-midnight */}
      <AbsoluteFill>
        <div style={{
          width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at 40% 60%, #0d0a18 0%, #1C1C1C 55%, #080808 100%)',
        }} />
      </AbsoluteFill>

      {/* Shape Layer — orbiting ellipse */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <ellipse cx="960" cy="540" rx="600" ry="300"
            fill="none" stroke="#FFC107" strokeWidth="1" opacity="0.05"
            transform={`rotate(${frame * 0.1}, 960, 540)`}
          />
          <ellipse cx="960" cy="540" rx="400" ry="200"
            fill="none" stroke="#e9d700" strokeWidth="1" opacity="0.04"
            transform={`rotate(-${frame * 0.07}, 960, 540)`}
          />
        </svg>
      </AbsoluteFill>

      {/* Motion Texture — sound wave lines */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
          {Array.from({ length: 8 }, (_, i) => {
            const points = Array.from({ length: 80 }, (_, j) => {
              const x = j * 24;
              const y = 540 + Math.sin(j * 0.3 + frame * 0.08 + i * 0.4) * (10 + i * 8);
              return `${x},${y}`;
            }).join(' ');
            return <polyline key={i} points={points} fill="none" stroke="#9E9E9E" strokeWidth="1" />;
          })}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {parts.map((p, i) => (
            <circle key={i}
              cx={p.x + Math.sin(frame * 0.04 + p.phase) * 16}
              cy={p.y + Math.cos(frame * 0.03 + p.phase) * 10}
              r={p.r} fill="#FFC107"
              opacity={0.07 + Math.sin(frame * 0.06 + p.phase) * 0.04}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* CENTER — Inner Voice Ripples */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', left: '50%', top: '50%', transform: 'translate(-50%, -50%)' }}>
          <svg width="300" height="300" viewBox="0 0 300 300">
            {/* Ripples */}
            {[ripple1, ripple2, ripple3].map((r, i) => (
              <circle key={i} cx="150" cy="150" r={r}
                fill="none" stroke="#e9d700"
                strokeWidth="2"
                opacity={Math.max(0, (1 - r / 180) * 0.3)}
              />
            ))}
            {/* Center heart/gut icon */}
            <path
              d="M 150 175 C 150 175 110 150 110 120 C 110 100 130 90 150 110 C 170 90 190 100 190 120 C 190 150 150 175 150 175 Z"
              fill="none" stroke="#e9d700" strokeWidth="3"
              opacity={0.6 + Math.sin(frame * 0.1) * 0.4}
            />
            {/* Sound waves from center */}
            <path
              d={`M 150 ${150 - waveAmp} Q ${150 + waveAmp * 0.6} 150 150 ${150 + waveAmp}`}
              fill="none" stroke="#FFC107" strokeWidth="2" opacity="0.4"
            />
          </svg>
        </div>
      </AbsoluteFill>

      {/* LEFT — "असुविधाजनक = न करो" */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 80, top: '50%',
          transform: `translateY(-50%) translateX(${xCardX}px)`,
          opacity: xCardOp,
          width: 700,
        }}>
          <div style={{
            background: 'linear-gradient(135deg, #1a0505, #2D2D2D)',
            border: '2px solid #ff000044',
            borderRadius: 24,
            padding: '48px 44px',
          }}>
            {/* Label */}
            <div style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 44,
              color: '#9E9E9E',
              fontWeight: 500,
              marginBottom: 20,
            }}>
              अगर यह लगे...
            </div>

            <div style={{
              fontFamily: 'Yantra One, sans-serif',
              fontSize: 80,
              fontWeight: 900,
              color: '#F5F5F5',
              lineHeight: 1.1,
              marginBottom: 32,
            }}>
              असुविधाजनक
            </div>

            {/* Arrow + NO */}
            <div style={{ display: 'flex', alignItems: 'center', gap: 24 }}>
              <svg width="60" height="24" viewBox="0 0 60 24">
                <defs>
                  <marker id="a6" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto">
                    <polygon points="0 0, 8 3, 0 6" fill="#ff0000" />
                  </marker>
                </defs>
                <line x1="0" y1="12" x2="52" y2="12" stroke="#ff0000" strokeWidth="3" markerEnd="url(#a6)" />
              </svg>

              <div style={{
                transform: `scale(${noReveal})`,
                opacity: interpolate(frame, [60, 72], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
                fontFamily: 'Yantra One, sans-serif',
                fontSize: 120,
                fontWeight: 900,
                color: '#ff0000',
                lineHeight: 1,
                textShadow: '0 0 30px rgba(255,0,0,0.4)',
              }}>
                न करो!
              </div>
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* RIGHT — "आंतरिक आवाज़ सुनो" */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', right: 80, top: '50%',
          transform: 'translateY(-50%)',
          opacity: rightOp,
          width: 780,
        }}>
          {/* Sound wave bars */}
          <div style={{ display: 'flex', justifyContent: 'center', gap: 8, marginBottom: 36 }}>
            {Array.from({ length: 18 }, (_, i) => {
              const h = 20 + Math.abs(Math.sin(frame * 0.12 + i * 0.5)) * 80;
              return (
                <div key={i} style={{
                  width: 10,
                  height: h,
                  background: `linear-gradient(180deg, #e9d700, #FFC10744)`,
                  borderRadius: 5,
                  alignSelf: 'center',
                }} />
              );
            })}
          </div>

          <div style={{
            textAlign: 'center',
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 100,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.15,
            marginBottom: 24,
          }}>
            आंतरिक<br />
            <span style={{ color: '#e9d700' }}>आवाज़</span><br />
            सुनो
          </div>

          <div style={{
            textAlign: 'center',
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 42,
            color: '#9E9E9E',
            fontWeight: 400,
            lineHeight: 1.5,
          }}>
            तुम्हारा अनुभव — सबसे बड़ा सलाहकार है
          </div>
        </div>
      </AbsoluteFill>

      {/* Stick Figure — ear to listen */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', right: 80, bottom: 130,
          opacity: interpolate(frame, [70, 88], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          transform: `translateY(${Math.sin(frame * 0.05) * 3}px) scale(${sp(70, { damping: 10 })})`,
        }}>
          <svg width="100" height="165" viewBox="0 0 100 165">
            <ellipse cx="50" cy="82" rx="44" ry="70" fill="#e9d700" opacity="0.07" />
            <circle cx="50" cy="24" r="20" fill="none" stroke="#F5F5F5" strokeWidth="2.8"
              style={{ transform: `rotate(${Math.sin(frame * 0.04) * 3}deg)`, transformOrigin: '50px 24px' }}
            />
            <rect x="33" y="20" width="12" height="8" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
            <rect x="53" y="20" width="12" height="8" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
            <line x1="45" y1="23" x2="53" y2="23" stroke="#e9d700" strokeWidth="1.8" />
            {/* Ear — tilted listening */}
            <path d="M 70 18 Q 85 24 85 30 Q 85 38 70 36" fill="none" stroke="#e9d700" strokeWidth="2" />
            <line x1="50" y1="44" x2="50" y2="105" stroke="#F5F5F5" strokeWidth="2.8" />
            {/* Hand on chin — thinking */}
            <line x1="50" y1="65" x2="22" y2="80" stroke="#F5F5F5" strokeWidth="2.8" />
            <line x1="50" y1="65" x2="72" y2="72" stroke="#F5F5F5" strokeWidth="2.8" />
            <circle cx="20" cy="82" r="5" fill="#F5F5F5" opacity="0.5" />
            <line x1="50" y1="105" x2="32" y2="142" stroke="#F5F5F5" strokeWidth="2.8" />
            <line x1="50" y1="105" x2="68" y2="142" stroke="#F5F5F5" strokeWidth="2.8" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.7) 100%)',
        }} />
      </AbsoluteFill>

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C8S6_AantarikAawaaz;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C8S7_ShantiAurBhavishya',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C8S7_ShantiAurBhavishya: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Two column reveal
  const col1Op = interpolate(frame, [10, 35], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const col1Y = interpolate(frame, [10, 35], [60, 0], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const col2Op = interpolate(frame, [30, 55], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const col2Y = interpolate(frame, [30, 55], [60, 0], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Balance beam / scale SVG
  const scaleOp = interpolate(frame, [55, 80], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Bottom insight
  const insightOp = interpolate(frame, [80, 105], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Particles
  const parts = Array.from({ length: 18 }, (_, i) => ({
    x: seededRandom(i * 4 + 1) * 1920,
    y: seededRandom(i * 4 + 2) * 1080,
    r: 2 + seededRandom(i * 4 + 3) * 5,
    phase: seededRandom(i * 4 + 4) * Math.PI * 2,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Background — balanced dual tone */}
      <AbsoluteFill>
        <div style={{
          width: '100%', height: '100%',
          background: 'linear-gradient(135deg, #0a1400 0%, #1C1C1C 35%, #1C1C1C 65%, #001020 100%)',
        }} />
      </AbsoluteFill>

      {/* Shape Layer — large yin-yang concept */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <circle cx="960" cy="540" r="500" fill="none" stroke="#e9d700" strokeWidth="1" opacity="0.04"
            transform={`rotate(${frame * 0.06}, 960, 540)`}
          />
          <path d="M 960 40 A 500 500 0 0 1 960 1040" fill="none" stroke="#9E9E9E" strokeWidth="1" opacity="0.05" />
          <rect x="100" y="900" width="300" height="180" rx="4" fill="#FFC107" opacity="0.03"
            transform={`rotate(${frame * 0.1}, 250, 990)`}
          />
        </svg>
      </AbsoluteFill>

      {/* Motion Texture — concentric hexagons */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.03 }}>
          {[200, 350, 500].map((r, i) => {
            const pts = Array.from({ length: 6 }, (_, j) => {
              const a = (j / 6) * Math.PI * 2;
              return `${960 + Math.cos(a) * r},${540 + Math.sin(a) * r}`;
            }).join(' ');
            return <polygon key={i} points={pts} fill="none" stroke="#F5F5F5" strokeWidth="0.8" />;
          })}
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {parts.map((p, i) => (
            <circle key={i}
              cx={p.x + Math.sin(frame * 0.04 + p.phase) * 14}
              cy={p.y + Math.cos(frame * 0.03 + p.phase) * 9}
              r={p.r} fill={i % 2 === 0 ? '#e9d700' : '#00cc66'}
              opacity={0.07 + Math.sin(frame * 0.05 + p.phase) * 0.04}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* ── TWO COLUMNS ── */}

      {/* LEFT — BACHHAT */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 80, top: 120,
          width: 820,
          opacity: col1Op,
          transform: `translateY(${col1Y}px)`,
        }}>
          {/* Icon */}
          <div style={{ marginBottom: 24 }}>
            <svg width="80" height="80" viewBox="0 0 80 80">
              <circle cx="40" cy="40" r="38" fill="none" stroke="#e9d700" strokeWidth="2.5" opacity="0.6" />
              {/* Moon — peace */}
              <path d="M 50 25 A 20 20 0 1 1 50 55 A 12 12 0 0 0 50 25 Z" fill="#e9d700" opacity="0.7" />
              {/* Stars */}
              <circle cx="60" cy="28" r="2.5" fill="#e9d700" />
              <circle cx="65" cy="42" r="2" fill="#e9d700" opacity="0.6" />
            </svg>
          </div>

          <div style={{
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 100,
            fontWeight: 900,
            color: '#e9d700',
            lineHeight: 1,
            marginBottom: 16,
          }}>
            बचत करो
          </div>

          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 48,
            color: '#F5F5F5',
            fontWeight: 600,
            marginBottom: 12,
            lineHeight: 1.3,
          }}>
            क्योंकि...
          </div>

          <div style={{
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 80,
            fontWeight: 900,
            color: '#e9d700',
            lineHeight: 1.1,
            marginBottom: 24,
          }}>
            शांती मिलती है
          </div>

          {/* Not because trend */}
          <div style={{
            background: '#2D2D2D',
            border: '1px solid #9E9E9E33',
            borderRadius: 14,
            padding: '18px 28px',
            opacity: interpolate(frame, [45, 65], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}>
            <span style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 36, color: '#9E9E9E',
            }}>
              न कि इसलिए कि <span style={{ color: '#ff0000', textDecoration: 'line-through' }}>सब करते हैं</span>
            </span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Divider */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: '50%', top: 100, bottom: 200,
          width: 2,
          background: 'linear-gradient(180deg, transparent, #e9d70044, #e9d700, #e9d70044, transparent)',
          opacity: interpolate(frame, [40, 60], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }} />
      </AbsoluteFill>

      {/* RIGHT — NIVESH */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', right: 80, top: 120,
          width: 820,
          opacity: col2Op,
          transform: `translateY(${col2Y}px)`,
        }}>
          {/* Icon */}
          <div style={{ marginBottom: 24 }}>
            <svg width="80" height="80" viewBox="0 0 80 80">
              <circle cx="40" cy="40" r="38" fill="none" stroke="#00cc66" strokeWidth="2.5" opacity="0.6" />
              {/* Shield */}
              <path d="M 40 15 L 62 25 L 62 45 C 62 58 40 68 40 68 C 40 68 18 58 18 45 L 18 25 Z" fill="none" stroke="#00cc66" strokeWidth="2.5" />
              <polyline points="30,40 38,48 55,32" fill="none" stroke="#00cc66" strokeWidth="2.5" strokeLinecap="round" />
            </svg>
          </div>

          <div style={{
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 100,
            fontWeight: 900,
            color: '#00cc66',
            lineHeight: 1,
            marginBottom: 16,
          }}>
            निवेश करो
          </div>

          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 48,
            color: '#F5F5F5',
            fontWeight: 600,
            marginBottom: 12,
            lineHeight: 1.3,
          }}>
            क्योंकि...
          </div>

          <div style={{
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 80,
            fontWeight: 900,
            color: '#00cc66',
            lineHeight: 1.1,
            marginBottom: 24,
          }}>
            भविष्य सुरक्षित होता है
          </div>

          {/* Not because trendy */}
          <div style={{
            background: '#2D2D2D',
            border: '1px solid #9E9E9E33',
            borderRadius: 14,
            padding: '18px 28px',
            opacity: interpolate(frame, [60, 80], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}>
            <span style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 36, color: '#9E9E9E',
            }}>
              न कि इसलिए कि <span style={{ color: '#ff0000', textDecoration: 'line-through' }}>ये ट्रेंडी है</span>
            </span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Bottom Balance Scale */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', bottom: 60, left: '50%',
          transform: 'translateX(-50%)',
          opacity: scaleOp,
        }}>
          <svg width="400" height="90" viewBox="0 0 400 90">
            {/* Fulcrum */}
            <polygon points="200,80 215,40 185,40" fill="#9E9E9E" opacity="0.5" />
            {/* Beam */}
            <line x1="60" y1="40" x2="340" y2="40" stroke="#e9d700" strokeWidth="3"
              transform={`rotate(${Math.sin(frame * 0.07) * 3}, 200, 40)`}
            />
            {/* Left pan */}
            <circle cx="80" cy="40" r="35" fill="none" stroke="#e9d700" strokeWidth="2" opacity="0.5" />
            <text x="80" y="45" textAnchor="middle" fill="#e9d700" fontSize="18" fontFamily="Noto Sans Devanagari">शांती</text>
            {/* Right pan */}
            <circle cx="320" cy="40" r="35" fill="none" stroke="#00cc66" strokeWidth="2" opacity="0.5" />
            <text x="320" y="45" textAnchor="middle" fill="#00cc66" fontSize="18" fontFamily="Noto Sans Devanagari">सुरक्षा</text>
          </svg>
        </div>
      </AbsoluteFill>

      {/* Stick Figure */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', right: 80, bottom: 140,
          opacity: interpolate(frame, [50, 68], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          transform: `translateY(${Math.sin(frame * 0.05) * 3}px) scale(${sp(50, { damping: 10 })})`,
        }}>
          <svg width="100" height="165" viewBox="0 0 100 165">
            <ellipse cx="50" cy="82" rx="45" ry="72" fill="#e9d700" opacity="0.07" />
            <circle cx="50" cy="24" r="20" fill="none" stroke="#F5F5F5" strokeWidth="2.8"
              style={{ transform: `rotate(${Math.sin(frame * 0.04) * 3}deg)`, transformOrigin: '50px 24px' }}
            />
            <rect x="33" y="20" width="12" height="8" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
            <rect x="53" y="20" width="12" height="8" rx="3" fill="none" stroke="#e9d700" strokeWidth="2" />
            <line x1="45" y1="23" x2="53" y2="23" stroke="#e9d700" strokeWidth="1.8" />
            <path d="M 43 33 Q 50 38 57 33" fill="none" stroke="#F5F5F5" strokeWidth="1.5" strokeLinecap="round" />
            <line x1="50" y1="44" x2="50" y2="105" stroke="#F5F5F5" strokeWidth="2.8" />
            <line x1="50" y1="65" x2="18" y2="80" stroke="#F5F5F5" strokeWidth="2.8" />
            <line x1="50" y1="65" x2="82" y2="80" stroke="#F5F5F5" strokeWidth="2.8" />
            <line x1="50" y1="105" x2="32" y2="142" stroke="#F5F5F5" strokeWidth="2.8" />
            <line x1="50" y1="105" x2="68" y2="142" stroke="#F5F5F5" strokeWidth="2.8" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.65) 100%)',
        }} />
      </AbsoluteFill>

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C8S7_ShantiAurBhavishya;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C8S8_KunalIntro',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C8S8_KunalIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Brand reveal — dramatic
  const brandScale = sp(15, { damping: 9, stiffness: 100 });
  const nameOp = interpolate(frame, [30, 55], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const nameY = interpolate(frame, [30, 55], [50, 0], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Map dots — India map suggestion
  const mapDots = Array.from({ length: 30 }, (_, i) => ({
    x: 700 + seededRandom(i * 3 + 1) * 700,
    y: 180 + seededRandom(i * 3 + 2) * 700,
    r: 3 + seededRandom(i * 3 + 3) * 4,
    delay: Math.floor(seededRandom(i * 7) * 60),
  }));

  // "वर्षों से" counter
  const yearsReveal = interpolate(frame, [65, 90], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Gold glow
  const glow = 0.4 + Math.sin(frame * 0.08) * 0.3;

  // Particles
  const parts = Array.from({ length: 20 }, (_, i) => ({
    x: seededRandom(i * 5 + 1) * 1920,
    y: seededRandom(i * 5 + 2) * 1080,
    r: 1.5 + seededRandom(i * 5 + 3) * 4,
    phase: seededRandom(i * 5 + 4) * Math.PI * 2,
    vy: -0.2 - seededRandom(i * 5 + 5) * 0.3,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Background — premium brand dark */}
      <AbsoluteFill>
        <div style={{
          width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at 50% 30%, #14100a 0%, #1C1C1C 50%, #080808 100%)',
        }} />
      </AbsoluteFill>

      {/* Shape Layer — broadcast frame */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {/* Corner accents */}
          <path d="M 60,60 L 60,160 M 60,60 L 160,60" stroke="#e9d700" strokeWidth="3" opacity="0.25" fill="none" />
          <path d="M 1860,60 L 1860,160 M 1860,60 L 1760,60" stroke="#e9d700" strokeWidth="3" opacity="0.25" fill="none" />
          <path d="M 60,1020 L 60,920 M 60,1020 L 160,1020" stroke="#e9d700" strokeWidth="3" opacity="0.25" fill="none" />
          <path d="M 1860,1020 L 1860,920 M 1860,1020 L 1760,1020" stroke="#e9d700" strokeWidth="3" opacity="0.25" fill="none" />

          {/* Gold bottom bar */}
          <rect x="0" y="1040" width="1920" height="4" fill="#e9d700" opacity="0.3" />
        </svg>
      </AbsoluteFill>

      {/* Motion Texture — radial scanlines */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.03 }}>
          {Array.from({ length: 24 }, (_, i) => {
            const a = (i / 24) * Math.PI * 2;
            return (
              <line key={i}
                x1="960" y1="540"
                x2={960 + Math.cos(a) * 1100} y2={540 + Math.sin(a) * 700}
                stroke="#F5F5F5" strokeWidth="0.5"
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* Rising particles */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {parts.map((p, i) => (
            <circle key={i}
              cx={p.x + Math.sin(frame * 0.03 + p.phase) * 15}
              cy={(p.y + frame * p.vy) % 1080}
              r={p.r} fill="#e9d700"
              opacity={0.1 + Math.sin(frame * 0.06 + p.phase) * 0.05}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* India map dots */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {mapDots.map((d, i) => (
            <circle key={i}
              cx={d.x}
              cy={d.y}
              r={d.r}
              fill="#e9d700"
              opacity={Math.max(0, interpolate(frame, [d.delay, d.delay + 20], [0, 0.25], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })) * (0.5 + Math.sin(frame * 0.05 + i) * 0.5)}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* ── MAIN CONTENT ── */}

      {/* LEFT — Brand Identity */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: 100, top: '50%',
          transform: 'translateY(-50%)',
          width: 700,
        }}>
          {/* ProBuffet HQ Logo treatment */}
          <div style={{
            transform: `scale(${brandScale})`,
            opacity: interpolate(frame, [0, 20], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            marginBottom: 40,
          }}>
            {/* PB Icon */}
            <div style={{
              display: 'flex', alignItems: 'center', gap: 18, marginBottom: 20,
            }}>
              <div style={{
                width: 80, height: 80,
                background: '#e9d700',
                borderRadius: 16,
                display: 'flex', alignItems: 'center', justifyContent: 'center',
                boxShadow: `0 0 ${30 * glow}px rgba(233,215,0,0.5)`,
              }}>
                <span style={{
                  fontFamily: 'Inter, sans-serif',
                  fontSize: 36, fontWeight: 900, color: '#1C1C1C',
                }}>
                  PB
                </span>
              </div>
              <div>
                <div style={{
                  fontFamily: 'Inter, sans-serif',
                  fontSize: 42, fontWeight: 900, color: '#F5F5F5',
                  letterSpacing: 2,
                }}>
                  ProBuffet
                </div>
                <div style={{
                  fontFamily: 'Inter, sans-serif',
                  fontSize: 32, fontWeight: 600, color: '#e9d700',
                  letterSpacing: 4,
                }}>
                  HQ
                </div>
              </div>
            </div>
          </div>

          {/* "मेरा नाम कुनाल है" */}
          <div style={{
            opacity: nameOp,
            transform: `translateY(${nameY}px)`,
          }}>
            <div style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 42, color: '#9E9E9E', fontWeight: 500,
              marginBottom: 8,
            }}>
              मेरा नाम
            </div>
            <div style={{
              fontFamily: 'Yantra One, sans-serif',
              fontSize: 160,
              fontWeight: 900,
              color: '#e9d700',
              lineHeight: 0.95,
              letterSpacing: -2,
              marginBottom: 20,
            }}>
              कुनाल
            </div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 42, color: '#9E9E9E', fontWeight: 500,
            }}>
              है
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* RIGHT — Journey Visual */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', right: 100, top: '50%',
          transform: 'translateY(-50%)',
          width: 900,
          opacity: interpolate(frame, [55, 80], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          {/* "वर्षों से" counter */}
          <div style={{
            textAlign: 'center',
            marginBottom: 36,
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 200,
              fontWeight: 900,
              color: '#F5F5F5',
              lineHeight: 1,
              opacity: yearsReveal,
              transform: `scale(${1 + (1 - yearsReveal) * 0.15})`,
            }}>
              वर्षों
            </div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 52,
              color: '#e9d700',
              fontWeight: 700,
            }}>
              देशभर के लोगों की वित्तीय यात्रा
            </div>
          </div>

          {/* Stats row */}
          <div style={{
            display: 'flex', gap: 20,
            opacity: interpolate(frame, [90, 115], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}>
            {[
              { val: 'हज़ारों', sub: 'परिवार', color: '#e9d700' },
              { val: 'सत्य', sub: 'कहानियां', color: '#FFC107' },
              { val: 'एक', sub: 'मकसद', color: '#F5F5F5' },
            ].map((s, i) => (
              <div key={i} style={{
                flex: 1,
                textAlign: 'center',
                background: '#2D2D2D',
                borderRadius: 18,
                padding: '28px 20px',
                border: `1px solid ${s.color}33`,
              }}>
                <div style={{
                  fontFamily: 'Yantra One, sans-serif',
                  fontSize: 56,
                  fontWeight: 900,
                  color: s.color,
                  lineHeight: 1,
                  marginBottom: 8,
                }}>
                  {s.val}
                </div>
                <div style={{
                  fontFamily: 'Noto Sans Devanagari, sans-serif',
                  fontSize: 32,
                  color: '#9E9E9E',
                }}>
                  {s.sub}
                </div>
              </div>
            ))}
          </div>
        </div>
      </AbsoluteFill>

      {/* Stick Figure — confident presenter */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', right: 80, bottom: 100,
          opacity: interpolate(frame, [40, 58], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          transform: `translateY(${Math.sin(frame * 0.05) * 3}px) scale(${sp(40, { damping: 10 })})`,
        }}>
          <svg width="110" height="180" viewBox="0 0 110 180">
            <ellipse cx="55" cy="90" rx="50" ry="78" fill="#e9d700" opacity={0.05 + glow * 0.04} />
            <circle cx="55" cy="24" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3"
              style={{ transform: `rotate(${Math.sin(frame * 0.04) * 3}deg)`, transformOrigin: '55px 24px' }}
            />
            <rect x="36" y="19" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <rect x="58" y="19" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <line x1="50" y1="23" x2="58" y2="23" stroke="#e9d700" strokeWidth="2" />
            <path d="M 44 33 Q 55 40 66 33" fill="none" stroke="#F5F5F5" strokeWidth="1.8" strokeLinecap="round" />
            <line x1="55" y1="46" x2="55" y2="115" stroke="#F5F5F5" strokeWidth="3" />
            {/* One arm pointing outward — presenter */}
            <line x1="55" y1="68" x2="95" y2="55" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="55" y1="68" x2="22" y2="88" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="55" y1="115" x2="36" y2="158" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="55" y1="115" x2="74" y2="158" stroke="#F5F5F5" strokeWidth="3" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 48%, rgba(0,0,0,0.7) 100%)',
        }} />
      </AbsoluteFill>

      {/* Watermark — PROMINENT this scene */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#e9d700',
        opacity: interpolate(frame, [0, 20], [0, 0.6], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C8S8_KunalIntro;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C8S9_KoiBhiNahiHila',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C8S9_KoiBhiNahiHila: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Three pillars reveal — staggered
  const pillars = [
    { title: 'सरलता', sub: 'अपनाते हो', delay: 15, color: '#e9d700' },
    { title: 'डर को', sub: 'समझते हो', delay: 35, color: '#FFC107' },
    { title: 'अर्थ', sub: 'जानते हो', delay: 55, color: '#F5F5F5' },
  ];

  // Storm vs mountain — big payoff
  const stormOp = interpolate(frame, [85, 110], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const mountainScale = sp(88, { damping: 9, stiffness: 80 });

  // Thunder lines
  const thunderOp = 0.4 + Math.sin(frame * 0.25) * 0.4;

  // Particles — flying debris effect
  const debris = Array.from({ length: 22 }, (_, i) => ({
    x: seededRandom(i * 6 + 1) * 1920,
    y: seededRandom(i * 6 + 2) * 1080,
    vx: (seededRandom(i * 6 + 3) - 0.5) * 4,
    vy: (seededRandom(i * 6 + 4) - 0.5) * 2,
    r: 2 + seededRandom(i * 6 + 5) * 5,
    phase: seededRandom(i * 6 + 6) * Math.PI * 2,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Background — storm-to-calm gradient */}
      <AbsoluteFill>
        <div style={{
          width: '100%', height: '100%',
          background: 'linear-gradient(160deg, #080808 0%, #1a1000 40%, #001218 100%)',
        }} />
      </AbsoluteFill>

      {/* Shape Layer — storm clouds top */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: stormOp * 0.4 }}>
          <ellipse cx="400" cy="100" rx="350" ry="120" fill="#2D2D2D" opacity="0.5"
            transform={`translate(${Math.sin(frame * 0.04) * 15}, ${Math.cos(frame * 0.03) * 8})`}
          />
          <ellipse cx="900" cy="60" rx="280" ry="100" fill="#2D2D2D" opacity="0.4"
            transform={`translate(${Math.sin(frame * 0.05 + 1) * 12}, ${Math.cos(frame * 0.04) * 6})`}
          />
          <ellipse cx="1500" cy="90" rx="320" ry="110" fill="#2D2D2D" opacity="0.45"
            transform={`translate(${Math.sin(frame * 0.03 + 2) * 10}, ${Math.cos(frame * 0.05) * 7})`}
          />
          {/* Lightning */}
          <polyline
            points="700,60 680,130 700,130 675,210"
            fill="none" stroke="#e9d700" strokeWidth="3"
            opacity={thunderOp * stormOp}
          />
          <polyline
            points="1300,40 1280,120 1300,120 1270,200"
            fill="none" stroke="#e9d700" strokeWidth="2.5"
            opacity={thunderOp * 0.8 * stormOp}
          />
        </svg>
      </AbsoluteFill>

      {/* Motion Texture — diagonal storm lines */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
          {Array.from({ length: 16 }, (_, i) => (
            <line key={i}
              x1={i * 130 - 200 + Math.sin(frame * 0.05) * 10}
              y1="0"
              x2={i * 130 + 200 + Math.sin(frame * 0.05) * 10}
              y2="1080"
              stroke="#9E9E9E" strokeWidth="1.5"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Moving debris particles */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute', opacity: stormOp }}>
          {debris.map((d, i) => (
            <circle key={i}
              cx={(d.x + d.vx * frame * 0.5) % 1920}
              cy={(d.y + d.vy * frame * 0.3) % 1080}
              r={d.r}
              fill="#9E9E9E"
              opacity={0.1 + Math.sin(frame * 0.07 + d.phase) * 0.05}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* ── THREE PILLARS (top section) ── */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', top: 80, left: 80, right: 80,
          display: 'flex', gap: 40,
        }}>
          {pillars.map((p, i) => (
            <div key={i} style={{
              flex: 1,
              opacity: interpolate(frame, [p.delay, p.delay + 20], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
              transform: `scale(${sp(p.delay, { damping: 9 })})`,
            }}>
              <div style={{
                background: `linear-gradient(135deg, #2D2D2D, #1a1a1a)`,
                border: `2px solid ${p.color}44`,
                borderRadius: 20,
                padding: '36px 32px',
                position: 'relative',
                overflow: 'hidden',
              }}>
                {/* Number badge */}
                <div style={{
                  position: 'absolute', top: 16, right: 16,
                  width: 36, height: 36,
                  background: p.color,
                  borderRadius: '50%',
                  display: 'flex', alignItems: 'center', justifyContent: 'center',
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: 20, fontWeight: 900, color: '#1C1C1C',
                }}>
                  {i + 1}
                </div>

                <div style={{
                  fontFamily: 'Yantra One, sans-serif',
                  fontSize: 80,
                  fontWeight: 900,
                  color: p.color,
                  lineHeight: 1,
                  marginBottom: 12,
                }}>
                  {p.title}
                </div>
                <div style={{
                  fontFamily: 'Noto Sans Devanagari, sans-serif',
                  fontSize: 38,
                  color: '#9E9E9E',
                  fontWeight: 500,
                }}>
                  {p.sub}
                </div>
              </div>
            </div>
          ))}
        </div>
      </AbsoluteFill>

      {/* ── MOUNTAIN — Unmoveable ── */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', bottom: 0, left: '50%',
          transform: `translateX(-50%) scale(${mountainScale})`,
          opacity: stormOp,
        }}>
          <svg width="900" height="400" viewBox="0 0 900 400">
            {/* Mountain shape */}
            <polygon points="450,20 800,380 100,380" fill="#2D2D2D" stroke="#e9d700" strokeWidth="2" />
            {/* Snow cap */}
            <polygon points="450,20 520,160 380,160" fill="#F5F5F5" opacity="0.8" />
            {/* "नहीं हिला सकता" */}
            <text x="450" y="300" textAnchor="middle"
              fill="#e9d700" fontSize="52" fontFamily="Yantra One, sans-serif" fontWeight="900"
            >
              नहीं हिला सकता
            </text>
          </svg>
        </div>
      </AbsoluteFill>

      {/* POWER LINE — "कोई भी बाजार" */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', bottom: 350, left: '50%',
          transform: 'translateX(-50%)',
          opacity: stormOp,
          textAlign: 'center',
          whiteSpace: 'nowrap',
        }}>
          <div style={{
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 130,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1,
            textShadow: '0 0 40px rgba(233,215,0,0.3)',
          }}>
            कोई भी <span style={{ color: '#ff0000' }}>बाजार</span>...
          </div>
        </div>
      </AbsoluteFill>

      {/* Stick Figure — standing firm on mountain */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: '50%', bottom: 380,
          transform: `translateX(-50%) translateY(${Math.sin(frame * 0.04) * 3}px)`,
          opacity: interpolate(frame, [88, 105], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          <svg width="90" height="150" viewBox="0 0 90 150">
            <ellipse cx="45" cy="75" rx="42" ry="68" fill="#e9d700" opacity="0.08" />
            <circle cx="45" cy="22" r="19" fill="none" stroke="#F5F5F5" strokeWidth="3"
              style={{ transform: `rotate(${Math.sin(frame * 0.04) * 3}deg)`, transformOrigin: '45px 22px' }}
            />
            <rect x="30" y="18" width="11" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2.2" />
            <rect x="48" y="18" width="11" height="7" rx="3" fill="none" stroke="#e9d700" strokeWidth="2.2" />
            <line x1="41" y1="21" x2="48" y2="21" stroke="#e9d700" strokeWidth="1.8" />
            {/* Determined expression */}
            <line x1="37" y1="32" x2="42" y2="30" stroke="#F5F5F5" strokeWidth="1.5" />
            <line x1="48" y1="30" x2="53" y2="32" stroke="#F5F5F5" strokeWidth="1.5" />
            <line x1="45" y1="41" x2="45" y2="100" stroke="#F5F5F5" strokeWidth="3" />
            {/* Arms crossed — confident */}
            <line x1="45" y1="62" x2="15" y2="75" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="45" y1="62" x2="75" y2="75" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="15" y1="75" x2="40" y2="72" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="75" y1="75" x2="50" y2="72" stroke="#F5F5F5" strokeWidth="2.5" />
            <line x1="45" y1="100" x2="28" y2="136" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="45" y1="100" x2="62" y2="136" stroke="#F5F5F5" strokeWidth="3" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.75) 100%)',
        }} />
      </AbsoluteFill>

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C8S9_KoiBhiNahiHila;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'C8S10_ShantiPurnJeevan',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const C8S10_ShantiPurnJeevan: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Sunrise animation
  const sunY = interpolate(frame, [10, 70], [700, 320], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const sunGlow = 0.6 + Math.sin(frame * 0.06) * 0.4;
  const sunRays = interpolate(frame, [30, 80], [0, 1], { easing: Easing.out(Easing.cubic), extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Age numbers — 80, 85, 90
  const age1Op = interpolate(frame, [5, 25], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const age2Op = interpolate(frame, [18, 38], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const age3Op = interpolate(frame, [31, 51], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Hero word reveal
  const heroScale = sp(55, { damping: 8, stiffness: 90 });
  const heroOp = interpolate(frame, [55, 75], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Tagline
  const tagOp = interpolate(frame, [85, 110], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Golden stars / fireflies
  const stars = Array.from({ length: 28 }, (_, i) => ({
    x: seededRandom(i * 5 + 1) * 1920,
    y: seededRandom(i * 5 + 2) * 1080,
    r: 1 + seededRandom(i * 5 + 3) * 3.5,
    phase: seededRandom(i * 5 + 4) * Math.PI * 2,
    twinkle: 0.3 + seededRandom(i * 5 + 5) * 0.7,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* Background — dawn sky gradient */}
      <AbsoluteFill>
        <div style={{
          width: '100%', height: '100%',
          background: `linear-gradient(180deg, 
            #080808 0%, 
            #0d0d18 20%,
            #1a0d05 40%,
            #241005 60%,
            #1C1C1C 100%
          )`,
        }} />
      </AbsoluteFill>

      {/* Sunrise glow */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', left: '50%', top: sunY - 100,
          transform: 'translateX(-50%)',
          width: 600, height: 600,
          borderRadius: '50%',
          background: `radial-gradient(circle, rgba(255,193,7,${0.15 * sunGlow}) 0%, rgba(233,215,0,${0.08 * sunGlow}) 40%, transparent 70%)`,
          pointerEvents: 'none',
        }} />
      </AbsoluteFill>

      {/* Sun SVG */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {/* Sun rays */}
          {Array.from({ length: 16 }, (_, i) => {
            const a = (i / 16) * Math.PI * 2;
            const innerR = 70;
            const outerR = 110 + Math.sin(frame * 0.1 + i * 0.4) * 15;
            return (
              <line key={i}
                x1={960 + Math.cos(a) * innerR} y1={sunY + Math.sin(a) * innerR}
                x2={960 + Math.cos(a) * outerR} y2={sunY + Math.sin(a) * outerR}
                stroke="#FFC107" strokeWidth="2.5"
                opacity={sunRays * (0.4 + Math.sin(frame * 0.08 + i) * 0.2)}
              />
            );
          })}

          {/* Sun circle — half above horizon */}
          <circle cx="960" cy={sunY} r="60"
            fill="#FFC107"
            opacity={interpolate(frame, [10, 40], [0, 0.9], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}
          />
          <circle cx="960" cy={sunY} r="45"
            fill="#F5F5F5"
            opacity={interpolate(frame, [10, 40], [0, 0.7], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}
          />
        </svg>
      </AbsoluteFill>

      {/* Horizon line */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <line x1="0" y1="640" x2="1920" y2="640"
            stroke="#e9d700" strokeWidth="1.5"
            opacity={interpolate(frame, [40, 60], [0, 0.2], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}
          />
        </svg>
      </AbsoluteFill>

      {/* Shape Layer — gentle curves */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          <path d="M 0,700 Q 480,620 960,660 Q 1440,700 1920,640" fill="none" stroke="#e9d700" strokeWidth="1" opacity="0.06" />
          <path d="M 0,720 Q 480,650 960,680 Q 1440,720 1920,660" fill="none" stroke="#FFC107" strokeWidth="1" opacity="0.04" />
        </svg>
      </AbsoluteFill>

      {/* Twinkling stars */}
      <AbsoluteFill>
        <svg width="1920" height="1080" style={{ position: 'absolute' }}>
          {stars.map((s, i) => (
            <circle key={i}
              cx={s.x}
              cy={s.y}
              r={s.r}
              fill="#e9d700"
              opacity={(0.1 + Math.sin(frame * 0.1 + s.phase) * 0.1) * s.twinkle}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* ── AGE COUNTER ROW ── */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', top: 80, left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex', gap: 40, alignItems: 'center',
        }}>
          {[
            { age: '80', op: age1Op },
            { age: '85', op: age2Op },
            { age: '90', op: age3Op },
          ].map((a, i) => (
            <React.Fragment key={i}>
              <div style={{
                opacity: a.op,
                transform: `scale(${sp(5 + i * 13, { damping: 8 })})`,
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 120,
                fontWeight: 900,
                color: i === 2 ? '#e9d700' : '#F5F5F5',
                lineHeight: 1,
                textShadow: i === 2 ? '0 0 30px rgba(233,215,0,0.4)' : 'none',
              }}>
                {a.age}
              </div>
              {i < 2 && (
                <div style={{
                  opacity: interpolate(frame, [25 + i * 13, 35 + i * 13], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: 60, color: '#9E9E9E',
                }}>
                  →
                </div>
              )}
            </React.Fragment>
          ))}
          <div style={{
            opacity: age3Op,
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 48, color: '#9E9E9E',
            marginLeft: 16,
            alignSelf: 'flex-end', marginBottom: 20,
          }}>
            साल
          </div>
        </div>
      </AbsoluteFill>

      {/* ── HERO MESSAGE ── */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', top: '50%', left: '50%',
          transform: `translate(-50%, -50%) scale(${heroScale})`,
          opacity: heroOp,
          textAlign: 'center',
          width: 1600,
        }}>
          <div style={{
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 170,
            fontWeight: 900,
            color: '#F5F5F5',
            lineHeight: 1.05,
            letterSpacing: -3,
            marginBottom: 16,
          }}>
            शांतिपूर्ण
          </div>
          <div style={{
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 170,
            fontWeight: 900,
            color: '#e9d700',
            lineHeight: 1.05,
            letterSpacing: -3,
          }}>
            जीवन
          </div>
        </div>
      </AbsoluteFill>

      {/* ── TAGLINE — bottom ── */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', bottom: 120, left: '50%',
          transform: 'translateX(-50%)',
          opacity: tagOp,
          textAlign: 'center',
          width: 1400,
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 54,
            fontWeight: 600,
            color: '#F5F5F5',
            lineHeight: 1.5,
            marginBottom: 8,
          }}>
            किसी भी <span style={{ color: '#ff0000' }}>संख्या</span> से...
          </div>
          <div style={{
            fontFamily: 'Yantra One, sans-serif',
            fontSize: 90,
            fontWeight: 900,
            color: '#e9d700',
            lineHeight: 1,
          }}>
            कहीं ज़्यादा कीमती है
          </div>
        </div>
      </AbsoluteFill>

      {/* Stick Figure — old & peaceful, rocking chair */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', right: 100, bottom: 100,
          opacity: interpolate(frame, [55, 72], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          transform: `
            translateY(${Math.sin(frame * 0.05) * 3}px)
            rotate(${Math.sin(frame * 0.04) * 2}deg)
            scale(${sp(55, { damping: 10 })})
          `,
        }}>
          <svg width="120" height="200" viewBox="0 0 120 200">
            <ellipse cx="60" cy="100" rx="55" ry="88" fill="#e9d700" opacity={0.05 + sunGlow * 0.04} />
            <circle cx="60" cy="26" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3"
              style={{ transform: `rotate(${Math.sin(frame * 0.04) * 3}deg)`, transformOrigin: '60px 26px' }}
            />
            <rect x="40" y="21" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <rect x="63" y="21" width="14" height="9" rx="4" fill="none" stroke="#e9d700" strokeWidth="2.5" />
            <line x1="54" y1="25" x2="63" y2="25" stroke="#e9d700" strokeWidth="2" />
            {/* Peaceful smile */}
            <path d="M 49 36 Q 60 44 71 36" fill="none" stroke="#F5F5F5" strokeWidth="2" strokeLinecap="round" />
            {/* Closed eyes */}
            <path d="M 47 23 Q 50 21 53 23" fill="none" stroke="#F5F5F5" strokeWidth="1.5" strokeLinecap="round" />
            <path d="M 65 23 Q 68 21 71 23" fill="none" stroke="#F5F5F5" strokeWidth="1.5" strokeLinecap="round" />
            <line x1="60" y1="48" x2="60" y2="120" stroke="#F5F5F5" strokeWidth="3" />
            {/* Relaxed arms */}
            <line x1="60" y1="72" x2="25" y2="95" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="60" y1="72" x2="95" y2="90" stroke="#F5F5F5" strokeWidth="3" />
            {/* Rocking chair base */}
            <line x1="60" y1="120" x2="40" y2="162" stroke="#F5F5F5" strokeWidth="3" />
            <line x1="60" y1="120" x2="80" y2="162" stroke="#F5F5F5" strokeWidth="3" />
            <path d="M 15,168 Q 60,155 105,168" fill="none" stroke="#e9d700" strokeWidth="3" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* End card bottom bar */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', bottom: 0, left: 0, right: 0,
          height: 6,
          background: 'linear-gradient(90deg, transparent, #e9d700, #FFC107, #e9d700, transparent)',
          opacity: interpolate(frame, [100, 120], [0, 0.8], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }} />
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ pointerEvents: 'none' }}>
        <div style={{
          width: '100%', height: '100%',
          background: 'radial-gradient(ellipse at center, transparent 42%, rgba(0,0,0,0.7) 100%)',
        }} />
      </AbsoluteFill>

      {/* Watermark — final, prominent */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#e9d700',
        opacity: interpolate(frame, [85, 110], [0, 0.5], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default C8S10_ShantiPurnJeevan;
```

